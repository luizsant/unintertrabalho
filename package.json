{
  "devDependencies": {
    "@testing-library/jest-dom": "^6.1.2",
    "@testing-library/react": "^14.0.0",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^29.6.4",
    "node-sass": "^9.0.0",
    "prettier": "^3.0.2",
    "sass": "^1.66.1"
  },
  "scripts": {
    "test": "jest && prettier --write .",
    "test:watch": "jest --watchAll",
    "compile-sass": "node-sass src/ -o src/"
  }
}
